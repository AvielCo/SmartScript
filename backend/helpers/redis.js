// const redis = require('redis');
// const client = redis.createClient({ port: 6379, host: '127.0.0.1' });

// client.on('error', (err) => console.log(`[Redis] ${err}`));
// client.on('connect', () => console.log('[Redis] Client connected to server...'));
// client.on('ready', () => console.log('[Redis] Client connected to redis and ready to use...'));
// client.on('end', () => console.log('[Redis] Client disconnected from redis.'));

// process.on('SIGINT', () => client.quit());

// module.exports = client;
